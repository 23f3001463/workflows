name: Example GitHub Action

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual triggering

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 23f3001463@ds.study.iitm.ac.in
        run: echo "Hello, world!"
      
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Another Step
        run: echo "This is another step"

